/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import RoutesClass from './src/components/RouteClass';

AppRegistry.registerComponent(appName, () => RoutesClass);
